# What is actually in the repos

Not nothing. Private NyxSpecter4/bountywarz already has worlds/drone-persian-gulf with combat, territory, contracts, pedagogy, overlay, world-boot geo projection.
Live /gulf 404s relative js/* because URL /gulf has no trailing slash. Files live at /worlds/drone-persian-gulf/js/.
vercel.json git.deploymentEnabled=false so pushes do not auto-go-live.
